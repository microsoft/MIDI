<Page
    x:Class="Microsoft.Midi.Settings.Views.EndpointsBasicLoopPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:Microsoft.Midi.Settings.Views"
    xmlns:local="using:Microsoft.Midi.Settings.ViewModels"
    xmlns:midi2="using:Microsoft.Windows.Devices.Midi2"
    xmlns:helpers="using:Microsoft.Midi.Settings.Helpers"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:midicontrols="using:Microsoft.Midi.Settings.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Uid="DevicesPage_Description" Margin="0,0,0,8" />

        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="0,0,8,0" HorizontalAlignment="Stretch">

                <!-- Transport -->


                <!-- If this transport supports runtime creation, have create button at this level -->


                <!--<TextBlock Text="{x:Bind ViewModel.Transport.Name, Mode=OneWay}" Style="{StaticResource SmallEmphasizedPropertyValue}"/>
                <TextBlock Text="{x:Bind ViewModel.Transport.Description, Mode=OneWay}" Style="{StaticResource SmallPlainPropertyValue}"/>-->

                <StackPanel Orientation="Horizontal">
                    <Button x:Name="CreateNewLoopback" 
                        Margin="5,13,13,13"
                        Content="Create New Basic Loopback Endpoint"
                        Click="CreateNewLoopback_Click"/>

                    <Button x:Name="CreateDefaultLoopback" 
                            Visibility="{x:Bind ViewModel.DefaultLoopbackExists, Mode=OneWay, Converter={StaticResource BooleanToInverseVisibilityConverter}}"
                            Margin="5,13,13,13"
                            Content="Create Default Basic Loopback Endpoint"
                            Command="{x:Bind ViewModel.CreateDefaultLoopbackCommand}"/>
                </StackPanel>

                <ItemsControl ItemsSource="{x:Bind ViewModel.MidiBasicLoopbackEndpoints, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        
                        <DataTemplate x:DataType="local:MidiBasicLoopbackEndpoint">
                            <controls:SettingsCard Margin="3" CornerRadius="8">
                                <controls:SettingsCard.Header>
                                    <Grid>
                                        <Grid HorizontalAlignment="Left"
                                              VerticalAlignment="Top"
                                              Margin="-6"
                                              Visibility="{x:Bind Loop.IsMuted, Converter={StaticResource BooleanToVisibilityConverter}}">

                                            <Ellipse Width="20" Height="20"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Fill="{StaticResource SystemFillColorCriticalBackgroundBrush}" />
                                            
                                            <FontIcon Glyph="&#xE74F;"
                                                      FontSize="14"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      />
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <ImageIcon Grid.Column="0" 
                                               Source="{x:Bind Loop.Image, Mode=OneWay}" 
                                               Width="60" Height="60"
                                               Margin="8"/>

                                            <StackPanel Orientation="Vertical"
                                                    VerticalAlignment="Center"
                                                    Grid.Column="1">
                                                <TextBlock Text="{x:Bind Loop.Name}" 
                                                       Style="{StaticResource SmallEmphasizedPropertyValue}"/>
                                                <TextBlock Text="{x:Bind Loop.Description}" 
                                                       Style="{StaticResource SmallPlainPropertyValue}"/>
                                            </StackPanel>



                                        </Grid>
                                    </Grid>
                                    
                                    
                                </controls:SettingsCard.Header>
                                <controls:SettingsCard.Content>
                                    <Grid HorizontalAlignment="Stretch">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" 
                                                   Text="{x:Bind Loop.Id}" 
                                                   Style="{StaticResource SmallPlainPropertyValue}"/>

                                        <StackPanel Grid.Row="1" 
                                                    Orientation="Horizontal"
                                                    HorizontalAlignment="Right">

                                            <Button Visibility="{x:Bind Loop.IsMuted, Converter={StaticResource BooleanToInverseVisibilityConverter}}"
                                                    Margin="8,8,8,8"
                                                    Command="{x:Bind MuteCommand}">
                                                <FontIcon Glyph="&#xE74F;"/>
                                            </Button>

                                            <Button Visibility="{x:Bind Loop.IsMuted, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                    Margin="8,8,8,8"
                                                    Command="{x:Bind UnmuteCommand}">
                                                <FontIcon Glyph="&#xE767;"/>

                                            </Button>

                                            <Button Margin="32,8,8,8"
                                                    Command="{x:Bind RemoveCommand}">
                                                <FontIcon Glyph="&#xE74D;"/>
                                            </Button>
                                        </StackPanel>


                                    </Grid>

                                </controls:SettingsCard.Content>
                               
                            </controls:SettingsCard>
                            
                        </DataTemplate>

                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
            </StackPanel>
        </ScrollViewer>


        <!-- Create Loopbacks UI -->

        <ContentDialog x:Name="Dialog_CreateLoopbackEndpoints"
                       PrimaryButtonText="OK" 
                       IsPrimaryButtonEnabled="{x:Bind ViewModel.NewLoopbackSettingsAreValid, Mode=OneWay}"
                       CloseButtonText="Cancel"
                       Closing="Dialog_CreateLoopbackEndpoints_Closing"
                       PrimaryButtonCommand="{x:Bind ViewModel.CreateLoopbackCommand}"
                       >
            <ContentDialog.Resources>
                <x:Double x:Key="ContentDialogMaxWidth">900</x:Double>
                <x:Double x:Key="ContentDialogMaxHeight">850</x:Double>
            </ContentDialog.Resources>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Orientation="Vertical" >
                    <TextBlock Text="Create Basic MIDI 1.0 Loopback Endpoint" 
                           Style="{StaticResource DialogHeaderTextBlockStyle}"
                           Margin="13, 9, 13, 13"/>

                    <TextBlock TextWrapping="WrapWholeWords" 
                           Text="This is a traditional style lookback where everything sent out to the destination (Output) port arrives in the source (Input) port. This was designed specifically for cases where MIDI 2.0 bidirectional support is not needed or desired. Any number of applications may use these loopback endpoints, and you may create as many loopback endpoints as you need."
                           Style="{StaticResource DialogDescriptionTextBlockStyle}" 
                           Margin="13"/>

                    <!-- I don't know why I need to do the crazy margins here. It renders properly outside of WinUI -->
                    <Image Height="650"
                           Width="650"
                           Stretch="Uniform"
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Margin="0,-250,0,-250">
                        <Image.Source>
                            <SvgImageSource UriSource="/Assets/BasicLoopback-Diagram.svg"/>
                        </Image.Source>
                    </Image>


                    <StackPanel x:Name="SetupPanel">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBox Grid.Column="0" 
                                 Grid.Row="0"
                                 VerticalAlignment="Top"
                                 Header="Loopback Name" 
                                 Width="250"
                                 Text="{x:Bind ViewModel.NewLoopbackEndpointName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 MaxLength="31"
                                 Margin="13"/>

                            <TextBox Grid.Column="1" 
                                 Grid.Row="0"
                                 VerticalAlignment="Top"
                                 Header="Description (Optional)" 
                                 Text="{x:Bind ViewModel.NewLoopbackEndpointDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Height="100"
                                 MaxLength="500"
                                 TextWrapping="Wrap"
                                 IsSpellCheckEnabled="True"
                                 Margin="13"/>
                        </Grid>

                        <TextBlock TextWrapping="WrapWholeWords" 
                           Text="All MIDI endpoints require a unique identifer. This can be any alpha-numeric text, no spaces or special characters, up to 32 characters long, that is unique within the endpoints for this transport. A default value has been created for you."
                           Style="{StaticResource DialogDescriptionTextBlockStyle}" 
                           Margin="13"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBox Header="Unique Identifier" 
                                 Text="{x:Bind ViewModel.NewUniqueIdentifier, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 MaxLength="32"
                                 Margin="13"/>

                            <!-- second column is empty -->
                        </Grid>
                    </StackPanel>
                  
                    <ToggleSwitch Margin="13"
                              IsEnabled="{x:Bind ViewModel.IsConfigFileActive, Mode=OneWay}"
                              IsOn="{x:Bind ViewModel.NewLoopbackIsPersistent, Mode=TwoWay}"
                              Header="Retain this loopback endpoint across service restarts/reboots. If this control is disabled, please create a configuration file using the options on the home page." 
                              />

                    <TextBlock Text="{x:Bind ViewModel.ValidationErrorMessage, Mode=OneWay}"
                           Margin="13"
                           Style="{StaticResource ValidationErrorTextBlockStyle}"
                           Visibility="{x:Bind ViewModel.NewLoopbackSettingsAreValid, Mode=OneWay, Converter={StaticResource BooleanToInverseVisibilityConverter}}" />


                </StackPanel>
            </ScrollViewer>
        </ContentDialog>


    </Grid>

</Page>
