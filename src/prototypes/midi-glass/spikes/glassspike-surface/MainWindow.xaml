<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="glassspike.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:glassspike"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Loaded="OnRootLoaded" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Margin="12,10,12,10" VerticalAlignment="Center">
            <TextBlock Text="Rendering approach" VerticalAlignment="Center" />
            <ComboBox x:Name="ModeCombo" Width="160" SelectedIndex="0" AutomationProperties.Name="Rendering approach">
                <ComboBoxItem Content="Templated XAML" Tag="xaml" />
                <ComboBoxItem Content="Composition" Tag="composition" />
                <ComboBoxItem Content="Hybrid" Tag="hybrid" />
            </ComboBox>

            <TextBlock Text="Controls" VerticalAlignment="Center" />
            <NumberBox x:Name="ControlCountBox" Value="200" Minimum="1" Maximum="2000" SpinButtonPlacementMode="Compact" Width="120" AutomationProperties.Name="Control count" />

            <TextBlock Text="Animating" VerticalAlignment="Center" />
            <NumberBox x:Name="AnimateCountBox" Value="12" Minimum="0" Maximum="2000" SpinButtonPlacementMode="Compact" Width="120" AutomationProperties.Name="Animating control count" />

            <TextBlock Text="Seconds" VerticalAlignment="Center" />
            <NumberBox x:Name="SecondsBox" Value="20" Minimum="1" Maximum="600" SpinButtonPlacementMode="Compact" Width="110" AutomationProperties.Name="Run length in seconds" />

            <Button x:Name="BuildButton" Content="Build page" Click="OnBuildClick" />
            <Button x:Name="RunButton" Content="Run measurement" Style="{ThemeResource AccentButtonStyle}" Click="OnRunClick" />
            <Button x:Name="ThemeButton" Content="Swap theme" Click="OnThemeClick" />
        </StackPanel>

        <Border Grid.Row="1" Margin="12,0,12,0" CornerRadius="8" Background="#101114" BorderThickness="1" BorderBrush="#26FFFFFF">
            <ScrollViewer x:Name="SurfaceScroller"
                          HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto"
                          VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto"
                          ZoomMode="Disabled">
                <Canvas x:Name="SurfaceCanvas" Width="1280" Height="800" Background="Transparent">
                    <Canvas.Resources>
                        <Style x:Key="GlassFaderStyle" TargetType="local:GlassControl">
                            <Setter Property="IsTabStop" Value="True" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="local:GlassControl">
                                        <Grid>
                                            <Border x:Name="PART_Bloom" Margin="-6" CornerRadius="11" Opacity="0" />
                                            <Border x:Name="PART_Plate" CornerRadius="7" BorderThickness="1" />
                                            <Rectangle x:Name="PART_Track" Width="8" RadiusX="4" RadiusY="4" Margin="0,8" HorizontalAlignment="Center" />
                                            <Rectangle x:Name="PART_Pipe" Width="8" RadiusX="4" RadiusY="4" Margin="0,8" Height="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <Style x:Key="GlassPadStyle" TargetType="local:GlassControl">
                            <Setter Property="IsTabStop" Value="True" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="local:GlassControl">
                                        <Grid>
                                            <Border x:Name="PART_Bloom" Margin="-6" CornerRadius="11" Opacity="0" />
                                            <Border x:Name="PART_Plate" CornerRadius="7" BorderThickness="1" />
                                            <Rectangle x:Name="PART_Track" Height="8" RadiusX="4" RadiusY="4" Margin="8" VerticalAlignment="Bottom" />
                                            <Rectangle x:Name="PART_Pipe" Height="8" RadiusX="4" RadiusY="4" Margin="8" Width="0" HorizontalAlignment="Left" VerticalAlignment="Bottom" />
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <Style x:Key="GlassKnobStyle" TargetType="local:GlassControl">
                            <Setter Property="IsTabStop" Value="True" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="local:GlassControl">
                                        <Grid>
                                            <Border x:Name="PART_Bloom" Margin="-6" CornerRadius="11" Opacity="0" />
                                            <Border x:Name="PART_Plate" CornerRadius="7" BorderThickness="1" />
                                            <Ellipse x:Name="PART_Ring" Margin="8" StrokeThickness="4" />
                                            <Canvas>
                                                <Path x:Name="PART_Arc" StrokeThickness="4" Stretch="None"
                                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                            </Canvas>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Canvas.Resources>
                </Canvas>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Margin="12,10,12,12" Spacing="6">
            <TextBlock x:Name="StatusText" Text="Ready." TextWrapping="Wrap" />
            <TextBox x:Name="ResultsText" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                     FontFamily="Cascadia Mono" FontSize="12" Height="180"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     AutomationProperties.Name="Measurement results" />
        </StackPanel>
    </Grid>
</Window>
